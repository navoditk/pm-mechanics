# Analytics Queries

Append-only log of `/pm-query` sessions (see `skills/pm-query/SKILL.md`).
One file per query, named `<YYYY-MM-DD>-<slug>.md`, written once and
never edited afterward: the question asked, the `src/pm` function(s)
called with their inputs, the result, and the reference page cited.
