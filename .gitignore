.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
*.egg-info/
.DS_Store
.env
site_src/
site/
.cache/

# Agent tooling -- per-developer state, never shared.
# `.claude/settings.json` IS tracked: it carries the team's shared permission
# baseline. Only the `.local.` variants are personal.
.claude/settings.local.json
.claude/*.local.json
